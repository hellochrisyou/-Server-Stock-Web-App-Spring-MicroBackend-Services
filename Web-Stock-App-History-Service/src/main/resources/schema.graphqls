schema{
 query: Query
 mutation : Mutation
}

scalar DateTime

type Query {
	findAllHistory(email: String): [BaseHistory]
}

type Mutation {
	addHistory(history: [HistoryInput]): BaseHistory
	clearHistory(email: String, type: String): [BaseHistory]
}

type BaseHistory {
	id: ID,
	email: String,
	title: String,
	type: String,
	
}

type Response_History {
	id: ID,
	data: [BaseHistory]
}

input HistoryInput {
	id: ID,
	email: String,
	title: String,
	type: String,	
	
}

